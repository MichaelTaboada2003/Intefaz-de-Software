<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>Inicio de sesión</title>
  </head>

<main>
    <div class="contenedor">
      <form id="loginForm">
        <h1>Login</h1>
        <div class="entrada">
          <input type="text" name="username" placeholder="Username" required autocomplete="off"/>
          <i class="bi bi-person-fill"></i>
        </div>
        <div class="entrada">
          <input
            type="password"
            name="password"
            placeholder="Password"
            required
          />
          <i class="bi bi-lock-fill"></i>
        </div>
        <div class="etiqueta">
          <label for="remember"><input type="checkbox" name="remember" id="remember" /> Remember me</label>
          <a href="#">Forgot your password?</a>
        </div>
        <div>
          <button type="submit" class="boton">Login</button>
        </div>
        <div class="registrarse">
          <p>Don't have an account? <a href="#"> Register</a></p>
        </div>
      </form>
    </div>
  </main>
  
<script>
document.getElementById('loginForm')!.onsubmit = function (event) {
    event.preventDefault();
    const usernameInput = document.querySelector('[name="username"]') as HTMLInputElement;  
    if (usernameInput && usernameInput.value) {  
        localStorage.setItem('username', usernameInput.value); 
        window.location.href = '/User'; 
        console.error('Username is required');
    }
};

document.addEventListener('DOMContentLoaded', () => {
    const form = document.getElementById('loginForm') as HTMLFormElement | null;
    if (form) {
      form.onsubmit = (event: Event) => {
        event.preventDefault();
        
        // Buscar el input del nombre de usuario asegurando que es de tipo HTMLInputElement
        const usernameInput = document.querySelector('[name="username"]') as HTMLInputElement | null;
        if (usernameInput && usernameInput.value) {
            localStorage.setItem('username', usernameInput.value);
            window.location.href = '/User';
        } else {
            console.error('Username is required');
        }
      };
    } else {
      console.error('LoginForm not found');
    }
});

</script>
  

<style>
  @import url("https://fonts.googleapis.com/css2?family=DM+Sans:opsz,wght@9..40,100;9..40,200;9..40,300;9..40,400;9..40,500;9..40,600;9..40,700&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100&display=swap");
  @import url("https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.min.css");
  body {
    background: url(https://i.postimg.cc/gkWv7Xs3/Trans-Caribe.jpg) no-repeat;
    min-height: 100vh;
    background-size: cover;
    background-position: center;
  }

  a {
    color: #fff;
  }

  .contenedor {
    width: 440px;
    border: 2px solid rgb(255, 255, 255, 0.2);
    box-shadow: 0 0px 10px rgba(0, 0, 0, 0.15);
    border-radius: 10px;
    padding: 30px 40px;
    background: transparent;
    backdrop-filter: blur(15px);
    margin: 20px auto;
    color: #fff;
    font-weight: 500;
    font-family: "Poppins", sans-serif;
  }

  .contenedor h1 {
    font-size: 36px;
    text-align: center;
  }

  .contenedor .entrada {
    position: relative;
    width: 100%;
    height: 50px;
    margin: 30px 0;
  }

  .entrada input {
    width: 100%;
    height: 100%;
    background: transparent;
    outline: none;
    border: 2px solid rgb(255, 255, 255, 0.2);
    border-radius: 40px;
    font-size: 16px;
    padding: 20px 45px 20px 20px;
    color:#ffffff;
  }

  .entrada input::placeholder {
    color: #fff;
  }

  .entrada i {
    position: absolute;
    right: 20px;
    top: 50%;
    transform: translateY(-50%);
    font-size: 20px;
  }

  .contenedor .etiqueta {
    display: flex;
    justify-content: space-between;
    font-size: 14.5px;
    margin: -15px 0 15px 0;
  }

  .etiqueta a {
    text-decoration: none;
  }

  .etiqueta a:hover {
    text-decoration: underline;
  }

  .contenedor .boton {
    width: 100%;
    height: 45px;
    background-color: #fff;
    border-radius: 40px;
    border: none;
    outline: none;
    box-shadow: 0 0px 10px rgba(0, 0, 0, 0.1);
    color: #333;
    font-weight: 600;
    font-size: 18px;
    transition: all 0.3s ease; 
    cursor: pointer; 
  }

  .contenedor .boton:hover {
    background-color: #ffffff; /* Color de fondo blanco en hover */
    color: #E3600F; /* Color de texto naranja en hover */
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.4); /* Sombra más pronunciada para efecto de elevación */
    transform: translateY(-2px); /* Ligero movimiento hacia arriba para dar sensación de levantamiento */
}

  
  .contenedor .registrarse {
    font-size: 14.5px;
    text-align: center;
    margin: 20px 0px 15px;
  }

  .registrarse a {
    text-decoration: none;
    font-weight: 700;
  }

  @media (max-width: 768px) {

    .contenedor {
        width: 90% !important;
        padding: 20px !important;
        margin: 50px auto !important;
    }
 
}


</style>
